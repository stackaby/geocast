name: geocast

services:
  geocast.dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - 5173:5173

#  geocast.prod:
#    build:
#      context: .
#      dockerfile: Dockerfile.prod
#    ports:
#      - 80:80

