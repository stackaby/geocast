{
  "scripts": {
    "dev:": "yarn --cwd frontend vite --host",
    "build:": "yarn --cwd frontend vite build",
    "preview:": "yarn --cwd frontend vite preview"
  },
  "type": "module",
  "packageManager": "yarn@4.13.0",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@yarnpkg/sdks": "^3.2.3",
    "typescript": "^6.0.3"
  }
}
